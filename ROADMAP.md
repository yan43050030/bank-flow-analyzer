# 路线图（ROADMAP）

针对**职务犯罪调查**的使用场景规划。优先级 = 投入产出比 + 调查实战价值。

> 调查员核心痛点：从一堆流水中**快速识别可疑信号** + **形成可交付的证据链**。
> 设计原则：宁少算不重复（保守取证）、可解释（每个判定都能追到原始交易）、可调阈值（不同案件标准不同）。

> **修改本路线图前**：先看 [CLAUDE.md](CLAUDE.md) 和 [docs/DESIGN_DECISIONS.md](docs/DESIGN_DECISIONS.md)，理解既有设计后再加新方向。

---

## 当前状态（v4.1.0）

| 组 | 已完成 | 待做 |
|---|--------|------|
| A | ✅ A1（对手）/ A2-min（现金画像）/ A3（任职期）/ A4（消费画像）/ A5（可疑度打分） | A2 完整版（消费/收入比、地点分布） |
| B | ✅ B3（可调阈值规则引擎） | ⏳ B1（多银行合并）/ B2（关联交易识别） |
| C | ✅ C1（代持识别）/ C2（证据包导出） | C3（审计日志） |
| D | ✅ D1（嫌疑人聚合）/ D3（异常时序）/ D4（关键时间点）/ D5（资产线索） | D2（N 跳资金链） |
| E | — | E1/E2 见下文 |
| F | — | F1/F2 见下文 |
| G | ✅ G1（联动接口）/ G2（转账-通话交叉）— v4.0.0 | G3 综合关联评分 |

---

## 优先级推荐（v3.1+）

下一阶段的最大价值方向：

1. **B1 多银行流水合并 + 资金链追踪** — 跨卡侦查的基础设施，是 D2/E2 的前置
2. **D1 嫌疑人画像聚合** — 按身份证号合并多张卡，从"卡分析"升级到"人画像"
3. **D2 N 跳资金链追踪** — 发现 A→B→C 多层过桥，找最终受益人
4. **E1 案件管理（SQLite 持久化）** — 跨次启动保留状态，避免每次重新导入
5. **C3 审计日志** — 调查程序合法性证据，给法庭用

---

## A 组：立即可做（v2.3）

基于现有流水字段，工作量小，调查实战价值高。

> **状态**：A1 / A2-min / A5 已在 v2.3 落地（2026-05-01）。A3 / A4 待开发。

### A1. 主要对手 Top N 排行 ✅ v2.3 已完成
- **输出**：每张卡按累计金额、笔数、净流向 三种维度分别排序，显示 Top 10
- **维度**：
  - 区分对公（含"公司/有限/股份"等关键词）/ 个人
  - 标注**双向对手**（既转入又转出 → 疑似过账/洗钱通道）
  - 单一对手集中度（HHI/基尼系数，识别"专户"特征）
- **数据依赖**：流水中的"对手账号"+"交易对手"字段（已有）
- **UI 位置**：单卡 Tab 新增"对手分析"子页

### A2-min. 现金画像（最小版） ✅ v2.3 已完成

> 完整版 A2 仍在规划：单笔金额分布直方图、消费/(申报)收入比、消费地点分布

### A2. 现金存取深度画像（完整版）
- **单笔金额分布直方图**：识别 **5万 / 20万** 反洗钱阈值附近的拆分交易
- **整数偏好**：统计"恰好 49,000 / 99,000 / 199,000"次数 → 主观规避反洗钱阈值的特征
- **时段分布**：22:00 后取现、节假日交易、周末活动占比
- **渠道分布**：ATM / 柜面 / 无卡存取 比例
- **数据依赖**：交易时间（已有）、交易类型（已有）

### A3. 任职期对比
- **输入**：用户手动输入任职起止日期（每个嫌疑人）
- **输出**：自动对比"任职前 / 任职中 / 任职后"三段：
  - 总资金量、月均流水
  - 消费水平（消费/收入比）
  - 主要对手结构变化
  - 大额交易频次
- **数据依赖**：流水日期（已有）+ 用户输入的任职日期
- **价值**：职务犯罪取证最关键的图表之一

### A4. 消费画像细分
- **多级分类**：
  - 餐饮（细分高端/普通） / 百货 / 酒店 / 旅游 / 医疗 / 教育 / 娱乐
  - **高端品牌识别**（关键词库：爱马仕、卡地亚、LV、Tiffany、迪奥、阿玛尼…）
- **指标**：
  - 高端消费频率与金额
  - 消费/(申报)收入比
  - 消费地点分布（如有 POS 终端商户名）
- **数据依赖**：交易对手 + 备注（已有），需要扩展关键词库

### A5. 可疑度打分 ✅ v2.3 已完成
- **逻辑**：把 A1-A4 各项 + 以下规则汇总成 0-100 分
  - 整数偏好次数
  - 5/20万阈值附近交易数
  - 深夜交易占比
  - 任职期内异常活动数
  - 单一对手集中度
- **输出**：每张卡一个标签 **低 / 中 / 高**
- **价值**：报告里高亮异常项，调查员快速分诊

---

## B 组：中期（v2.4 候选）

需要中等工作量 / 数据扩展。

### B1. 多银行流水合并 + 资金链追踪 ⏳ 待实现

> v2.5.0 commit message 提及 "B1 基础"，但实际仅落地了 B3。B1 仍未实现。
- **当前**：v2.3 规划已提及（README "版本规划"）
- **能力**：
  - 跨卡识别同一对手账户的资金流向 A → B → C
  - **桑基图**（资金流向可视化）
  - **时间线热力图**（交易密度 × 时间）
- **数据依赖**：多张卡的流水合并、对手账号匹配

### B2. 关联交易识别
- **同一时间窗内多卡协同**：
  - 多张卡同时收到等比例资金（典型分账模式）
  - 同对手在多张卡之间频繁出现 → **关系网络图**
- **目的**：发现共谋、分赃、上下游关系

### B3. 可调阈值规则引擎
- **配置项**：
  - 大额阈值（5万 / 20万 / 100万 自定义）
  - 时段标红规则（22:00-6:00 / 节假日 / 周末）
  - 对手关键词黑名单（如"博彩、虚拟币、第三方支付聚合"）
- **目的**：调查员习惯不同，硬编码僵化；不同案件标准不同

---

## C 组：长期（v3.0 候选）

需外部数据接入，但是**核心调查能力**。

### C1. 办理网点 + 行程比对识别代持卡 ⭐
**这是调查员最想要的"杀手级"功能。**

- **数据来源**：
  - 流水里的"办理机构"字段（需要先做多银行模板适配）
  - 用户导入的行程表（机票 / 高铁 / 酒店登记 / 出入境）
  - 银行网点经纬度库

- **识别信号组合**：
  | 信号 | 说明 |
  |------|------|
  | S1 | 同一网点短期内办理多张不同户主的卡 |
  | S2 | 卡主户籍/工作地与办卡网点距离异常 |
  | S3 | 卡主行程与卡的活动时间冲突（卡主在 A 城，但卡在 B 城 ATM 取现） |
  | S4 | 卡的"行为画像"匹配另一个人（操作时段、消费偏好） |
  | S5 | 卡仅有"工资 + 取现/转出"模式，无主动消费 |
  | S6 | 卡的资金最终都流向同一受益人 |

- **输出**：每张卡的**代持嫌疑分**（加权打分）+ 嫌疑等级 + 证据链清单
- **难点**：不在算法，在**数据规范**——每家银行流水"办理网点"字段名不一样，需要适配层

### C2. 证据包导出
- **一键导出某张卡的调查报告**（PDF / Word）
- **包含**：
  - 资金量摘要 + 可疑度评级
  - 主要对手 / 时段 / 消费 图表
  - 可疑交易明细列表（带备注解释为什么标红）
  - 原始凭证位置索引（供检察官回溯查证）
- **价值**：给检察官 / 纪委的最终交付物

### C3. 审计日志
- **记录**：每次导入 / 查询 / 导出 / 阈值修改
- **价值**：办案过程本身可作为程序合法性证据；防止"事后篡改分析结果"质疑

---

## D 组：嫌疑人维度拓展（v3.1+ 新增）

把分析单位从"一张卡"升级到"一个人"或"一条资金链"，更贴合真实办案。

### D1. 嫌疑人画像聚合 ✅ v4.2.0 已完成
- 按持卡人身份证号（无则姓名）聚合多张卡 → `SuspectReport`
- **合并资金量剔除卡间互转**：本人 A 卡转 B 卡不算新资金
- 聚合合并交易数 / 资金量 / 消费收支 / 多卡最高可疑度·代持分
- UI 新增「👤 嫌疑人」Tab
- 后续可加：合并对手网络、合并任职期对比（当前 v1 未做）

### D2. N 跳资金链追踪 ⭐
- **场景**：A 给 B 转账→24 小时内 B 给 C 转账→C 给 D ... 自动追踪资金最终流向
- **识别信号**：
  - 时间窗口（默认 24 小时，可调）
  - 金额相似度（B 收到 100 万，给 C 转 99.5 万，扣 0.5 万"过桥费"）
  - 跳数（默认追 5 跳）
- **数据依赖**：D1（身份证聚合）+ B1（多银行合并）。需要多张卡的完整流水，单卡数据看不到链。
- **输出**：资金链桑基图 + 最终受益人列表
- **价值**：识别"过账户"洗钱模式，找真实控制人

### D3. 异常时序检测 ✅ v4.1.0 已完成
- **拆分洗钱**：大额入账（≥20万）后 24h 内拆分 ≥3 笔小额（<5万）流出
- **批量整数**：≥3 笔相同整额流出
- **节假日突击**：固定节日（元旦/劳动节/国庆）大额交易
  —— 农历节日（春节/中秋）需农历表，暂未覆盖（后续可加 `chinese_calendar` 库）
- **同步分赃**（跨卡）：多张卡同一时间窗收到大额入账 → `detect_synchronized_inflow()`
- 实现说明：D3 作为**独立异常清单**展示，不并入 A5 可疑度评分
  （理由：保持 A5 评分稳定，避免回归；详见 DESIGN_DECISIONS.md）

### D4. 关键时间点关联 ✅ v4.1.0 已完成
- 用户输入"招标日 / 合同签订日 / 调岗日"，自动扫 ±15 天窗口内交易
- 输出每个关键点的「前/后」交易笔数、金额、大额笔数

### D5. 资产线索识别 ✅ v4.3.0 已完成
- 从消费/缴费记录反推房产、车辆
- 三层关键词：强特征(高置信度) / 弱特征(需复核) / 排除词(否决误报)
- 房产：物业/水电燃气供热/房产交易/契税；车辆：4S店/车险/违章停车维保
- 结果聚合到 `SuspectReport`，单卡「🏠 资产线索」子 Tab 展示
- 后续可扩充关键词库（各地供电/税务的具体名称写法）

---

## E 组：持久化与协作（v3.2+ 新增）

跨次启动保留状态，多人协作。

### E1. 案件管理（SQLite 本地存档）
- **当前问题**：每次启动都要重新导入流水、重新分析。多张卡分散在多个文件难以管理
- **方案**：
  - 本地 SQLite（不联网，与"数据不离机"原则一致）
  - 存储结构：
    - `cases` 表（案件元信息）
    - `suspects` 表（嫌疑人 + 身份证 + 关联卡列表）
    - `cards` 表（卡号 + 流水来源）
    - `transactions` 表（原始交易，索引建在卡号+日期）
    - `analyses` 表（分析快照：可疑度/代持分/资金量等）
- **UI**：左侧新增"案件树"，可切换不同案件，自动恢复上次分析状态
- **加密**：SQLite 文件用 SQLCipher 加密，密码用户输入

### E2. 团伙关系图
- **场景**：基于多张卡的对手交集，自动生成嫌疑人 + 受益人 + 中间账户的网络图
- **算法**：NetworkX 构图，节点 = 账户/嫌疑人，边权 = 资金流量。识别社区（k-core / Louvain）
- **输出**：交互式关系图（HTML），可点击查看任意节点的详细流水
- **数据依赖**：D1（身份证聚合）+ B1（多银行合并）+ E1（持久化）

### E3. 多人协作（远期，谨慎评估）
- **想法**：本地服务器版，调查组共享案件数据
- **风险**：暴露面增大、需考虑认证授权、可能违反"数据不离机"原则
- **建议**：远期再考虑，目前优先单机加密

---

## F 组：调查员体验（v3.x 持续优化）

### F1. 增量分析
- **场景**：已分析过的卡导入新流水时，仅算增量交易，不重跑全部
- **实现**：基于 E1 持久化，记录上次分析的最大日期，仅处理之后的交易
- **价值**：大数据量场景下提速 100x

### F2. 快照比对
- **场景**：保存某时刻的分析快照（如初次分析），后续导入新流水后与快照比对差异
- **输出**：差异报告（新增对手 / 新增可疑模式 / 资金量增长 / 任职期资金量变化等）
- **价值**：嫌疑人后续动态监控；新增证据可视化

### F3. 自然语言查询（远期，依赖 LLM API）
- **想法**：调查员输入"找出 2024 年所有给王某转账超过 5 万的卡"，自动生成查询
- **风险**：LLM 调用涉及外部 API，违反"纯本地"原则
- **建议**：仅作为本地小模型部署的远期方向（如 Qwen-7B 本地推理）

---

## G 组：跨软件联动（v4.0.0 落地）

与**话单分析软件 (Call-record-analyzer)** 联动，叠加"资金关系网"和"通讯关系网"。
接口规范见 [docs/INTEROP_SPEC.md](docs/INTEROP_SPEC.md)。

### G1. 联动数据接口 ✅ v4.0.0 已完成
- `case-interop-v1` JSON 交换包：读 / 写 / schema 校验
- `interop.py` 模块；`Transaction` 增加 `counterparty_phone` 等联动字段
- UI：导入话单联动包 / 导出案件联动包 + 对手手机号等字段映射

### G2. 转账-通话时序交叉分析 ✅ v4.0.0 已完成
- 银行视角：「大额转账前 N 小时的密集通话」
- 识别受贿"先沟通→后送钱"铁证模式
- UI：「🔗 通联交叉」Tab

### G3. 综合关联评分 ⏳ 待实现
- 资金往来频次 + 通话频次 → 核心关系综合评分
- 建议放在导入了完整交换包的一侧算，结果写进 `analysis_summary`
- 依赖 D1（嫌疑人画像聚合）做实体对齐

---

## 数据安全配套（贯穿所有版本）

不属于功能，但每个版本都要遵守：

- **真实案件数据绝不入 Git**（已落实于 `.gitignore`）
- **本地加密盘存案件数据**（建议外置硬盘 + VeraCrypt / BitLocker / FileVault）
- **导出文件命名脱敏**（不含真实姓名/案号）
- **查询日志加密**（C3 落地后）

---

## 待确认事项

- [ ] 是否需要支持**多嫌疑人对比**（同案多卡主统一在一个视图里）？
- [ ] 行程数据的标准格式（用户自己整理 CSV，还是从某个系统导出）？
- [ ] 网点经纬度库从哪来（人民银行公开 / 自行收集 / 商业数据库）？
- [ ] 报告输出语言风格（公文 / 技术 / 折中）？
- [ ] 是否需要内网部署版（无外网环境运行）？
